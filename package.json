{
  "name": "chatgpt-chrome-ui-changer",
  "version": "0.1.0",
  "private": true,
  "description": "Responsive wide layout for ChatGPT conversations.",
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "typecheck": "tsc -p tsconfig.content.json --noEmit && tsc -p tsconfig.popup.json --noEmit && tsc -p tsconfig.test.json --noEmit",
    "build:ts": "tsc -p tsconfig.content.json && tsc -p tsconfig.popup.json",
    "build": "npm run clean && npm run build:ts && node scripts/copy-static.mjs",
    "test": "npm run build && tsc -p tsconfig.test.json && node --test .test-dist/*.test.js"
  },
  "devDependencies": {
    "typescript": "5.8.3"
  }
}
